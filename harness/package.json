{
  "name": "pvlab-harness",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "gen": "bun scripts/gen-registry.ts",
    "build": "bun scripts/gen-registry.ts && bun build --compile --target=bun-linux-x64 src/run.ts --outfile dist/pvlab"
  }
}