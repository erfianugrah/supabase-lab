# Facts from audit-integrity - 2026-09-08 05:31:57 UTC

Source artifact: run started 2026-09-08T05:31:57.518Z, finished 2026-09-08T05:36:24.081Z, vantage local, region ap-southeast-1, lab commit 60e7194. Project ref omitted on purpose.

## A00 - control: fresh project healthy, keys present, version recorded (pass)

| key | value |
|---|---|
| healthy | true |
| health_wait_s | 0 |
| pg_version | 17.6 |
| project_age_min | 42 |
| key_generations | 4 |

detail: health: auth=ACTIVE_HEALTHY rest=ACTIVE_HEALTHY db=ACTIVE_HEALTHY after 0s. Keys: anon, service_role, publishable, secret. Postgres 17.6, project 42 min old at first probe.

## A07a - audit and log retention per plan, from the entitlement API (pass)

| key | value |
|---|---|
| orgs_probed | 3 |
| team_plan | team |
| team_audit_log_days | 62 days |
| team_log_retention_days | 28 days |
| team_backup_retention_days | 14 days |
| team_audit_log_drains | true |
| team_log_drains | true |
| pro_plan | pro |
| pro_audit_log_days | no-access:0 days |
| pro_log_retention_days | 7 days |
| pro_backup_retention_days | 7 days |
| pro_audit_log_drains | false |
| pro_log_drains | true |
| free_plan | free |
| free_audit_log_days | no-access:0 days |
| free_log_retention_days | 1 days |
| free_backup_retention_days | no-access:0 days |
| free_audit_log_drains | false |
| free_log_drains | false |

detail: team (plan=team, HTTP 200, 64 entitlements): security.audit_logs_days=62 days; log.retention_days=28 days; backup.retention_days=14 days; audit_log_drains=true; log_drains=true; pitr.available_variants=pitr_7\|pitr_14\|pitr_28; security.private_link=true; security.soc2_report=true \|\| pro (plan=pro, HTTP 200, 64 entitlements): security.audit_logs_days=no-access:0 days; log.retention_days=7 days; backup.retention_days=7 days; audit_log_drains=false; log_drains=true; pitr.available_variants=pitr_7\|pitr_14\|pitr_28; security.private_link=false; security.soc2_report=false \|\| free (plan=free, HTTP 200, 64 entitlements): security.audit_logs_days=no-access:0 days; log.retention_days=1 days; backup.retention_days=no-access:0 days; audit_log_drains=false; log_drains=false; pitr.available_variants=none; security.private_link=false; security.soc2_report=false

## A07b - the platform audit log on the Management API (info)

| key | value |
|---|---|
| spec_paths | 115 |
| audit_paths | 0 |

detail: 115 paths in the /v1 spec; 0 mention audit - the platform audit log has no read path, no export path and no delete path on the public API. It is Dashboard-only, which is why no tenant credential can rewrite it and also why nobody can pull it into a SIEM.

## A11a - logged statements in the last hour, by role and application_name (info)

| key | value |
|---|---|
| groups | 2 |
| apps | postgres/mgmt-api x14 \| postgres/supabase/dashboard-query-editor x1 |

detail: postgres/mgmt-api x14, postgres/supabase/dashboard-query-editor x1. Every path this harness uses arrives as one role, so a per-person question cannot be answered from Postgres alone.

## A11b - a Dashboard SQL Editor session, if the operator step has been run (pass)

| key | value |
|---|---|
| operator_step_found | true |
| non_mgmt_groups | 1 |

detail: found the operator statement: role=postgres, application_name=supabase/dashboard-query-editor. That is the identity a Dashboard member's SQL runs as.

## A06a - audit-log keys in the documented auth config (info)

| key | value |
|---|---|
| config_keys | 243 |
| audit_keys | 1 |
| get_status | 200 |
| key_present_before | true |
| key_before | true |

detail: GET config/auth -> HTTP 200, 243 keys returned, 1 of them mention audit (audit_log_disable_postgres=true). audit_log_disable_postgres is present and reads true.

## A06b - PATCH audit_log_disable_postgres through the Management API (info)

| key | value |
|---|---|
| patch_status | 200 |
| patch_target | false |
| key_after_patch | true |
| write_took | false |

detail: asked for audit_log_disable_postgres=false (the opposite of the true it held). PATCH -> HTTP 200; on re-read the key is true. A 2xx that leaves the value unchanged: the switch is accepted and ignored on the public API, so it is Dashboard-only (Authentication -> Configuration -> Audit Logs).

## A06c - does a new auth event still reach the table (pass)

| key | value |
|---|---|
| create_status | 200 |
| login_status | 200 |
| create_attempts | 1 |
| rows_before | 0 |
| rows_after | 0 |
| tagged_row_appeared | false |
| postgres_copy_default | off |

detail: admin user create -> HTTP 200 in 1 attempt(s); password login -> HTTP 200. Table rows 0 -> 0; a row tagged with this run did not appear within 121s. On a fresh project the Postgres copy is OFF by default.

## A06z - cleanup (pass)

_no measurements_

detail: auth config PATCH reverted; test user deleted

## A09a - install a chained mirror fed by a trigger on the audit table (pass)

| key | value |
|---|---|
| install_ok | true |
| triggers_on_audit_table | 1 |

detail: schema, chained table, SECURITY DEFINER capture function, verifier and trigger created; 1 trigger now on auth.audit_log_entries. A tenant CAN attach a trigger to the Auth server's own table - the ACL grants postgres the TRIGGER privilege.

## A09b - a real auth event reaches the mirror through the trigger (fail)

| key | value |
|---|---|
| create_status | 200 |
| login_status | 200 |
| mirrored | false |
| mirror_lag_s | 125 |
| mirror_rows | 0 |
| verify_checked | 0 |
| verify_break | none |

detail: admin user create -> HTTP 200, password login -> HTTP 200; the mirror captured nothing within 125s. Mirror holds 0 rows (seq null..null); verifier: 0 rows checked, first break none. GoTrue writes as supabase_auth_admin and the SECURITY DEFINER function still records it.

## A09c - delete one mirror row: does the verifier notice (info)

| key | value |
|---|---|
| deleted_seq | 0 |
| verify_break_after_delete | none |
| verify_checked | 0 |

detail: could not delete a mirror row: no second row to delete

## A09d - the same identity rebuilds the chain and the verifier goes quiet (pass)

| key | value |
|---|---|
| rebuild_ok | true |
| verify_break_after_rebuild | none |
| head_hash_changed | false |
| head_before |  |
| head_after |  |

detail: rehash of every row -> true; verifier now reports first break none over 0 rows. The head hash moved from ... to ..., so the ONE thing that survives an owner with a rehash loop is a head hash that was copied out of the database before the tampering. In-database immutability against the database owner does not exist; off-box anchoring is the control.

## A09z - cleanup (pass)

_no measurements_

detail: trigger and schema dropped (0 user triggers left on the audit table); test user deleted
