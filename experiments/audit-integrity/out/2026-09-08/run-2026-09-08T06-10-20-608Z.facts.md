# Facts from audit-integrity - 2026-09-08 06:10:20 UTC

Source artifact: run started 2026-09-08T06:10:20.608Z, finished 2026-09-08T06:13:23.781Z, vantage local, region ap-southeast-1, lab commit 60e7194. Project ref omitted on purpose.

## A11a - logged statements in the last hour, by role and application_name (info)

| key | value |
|---|---|
| groups | 3 |
| apps | postgres/mgmt-api x13 \| postgres/supabase/dashboard x6 \| postgres/supabase/dashboard-query-editor x1 |

detail: postgres/mgmt-api x13, postgres/supabase/dashboard x6, postgres/supabase/dashboard-query-editor x1. Every path this harness uses arrives as one role, so a per-person question cannot be answered from Postgres alone.

## A11b - a Dashboard SQL Editor session, if the operator step has been run (pass)

| key | value |
|---|---|
| operator_step_found | true |
| non_mgmt_groups | 2 |

detail: found the operator statement: role=postgres, application_name=supabase/dashboard-query-editor. That is the identity a Dashboard member's SQL runs as.

## A05a - log sources on this project, and whether auth audit events have their own (pass)

| key | value |
|---|---|
| sources | 9 |
| auth_audit_logs_present | true |
| stream_error | no |

detail: 9 sources in the last 3 h: postgrest_logs, auth_logs, postgres_logs, supavisor_logs, pgbouncer_logs, edge_logs, realtime_logs, storage_logs, auth_audit_logs. auth_audit_logs is present.

## A05b - auth events reach auth.audit_log_entries, and which actions GoTrue records (pass)

| key | value |
|---|---|
| users_created | 3 |
| logins_ok | 3 |
| table_rows | 6 |
| table_lag_s | 0 |
| actions | 2 |

detail: 3/3 users created, 3 password logins. 6 rows in auth.audit_log_entries carry the run tag (first row 0s after the writes): login x3, user_signedup x3. The in-database copy is ENABLED on this project.

## A05c - the same auth events in the auth_audit_logs source (pass)

| key | value |
|---|---|
| stream_found | true |
| stream_rows | 6 |
| stream_lag_s | 33 |

detail: found 6 matching rows in auth_audit_logs, first visible 33s after the writes. This copy is written by the platform, not by the tenant's database, and no tenant credential has a write path to it.

## A05d - a forged entry: in the table, not in the stream (pass)

| key | value |
|---|---|
| forge_ok | true |
| forge_in_table | 1 |
| forge_in_stream | false |
| forge_search_s | 126 |

detail: forged insert -> true; the table now holds 1 row for that actor; the stream holds none after 126s, so the stream is fed by the Auth server and not by the table. A row in one store with no counterpart in the other is the signature - in both directions.

## A05e - delete the in-database copy; count both stores again (pass)

| key | value |
|---|---|
| delete_ok | true |
| table_before | 6 |
| table_after | 0 |
| stream_before | 6 |
| stream_after | 6 |
| divergence | 6 |
| postgres_copy_enabled | true |

detail: DELETE -> true. Table rows for the tag: 6 before, 0 after. Stream rows for the same tag: 6 before, 6 after. The erasure is complete in the store the tenant owns and invisible in the store the platform owns, so comparing the two counts for one window detects it.

## A05z - cleanup (pass)

_no measurements_

detail: 3 auth users deleted; tagged audit rows already removed by A05e

## A08a - a fresh auth event through GET /auth/v1/admin/audit (pass)

| key | value |
|---|---|
| login_status | 200 |
| endpoint_status | 200 |
| entries | 5 |
| tagged_entries | 2 |
| table_rows | 2 |
| endpoint_lag_s | 0 |

detail: user create -> HTTP 200, password login -> HTTP 200. GET /auth/v1/admin/audit (service key) -> HTTP 200, 5 entries, 2 carrying this run's tag (visible 0s after the write). The same event has 2 rows in auth.audit_log_entries (in-database copy enabled).

## A08b - the same read after the table rows are deleted (info)

| key | value |
|---|---|
| delete_ok | true |
| table_after | 0 |
| endpoint_tagged_before | 2 |
| endpoint_tagged_after | 0 |
| endpoint_is_table_window | true |
| postgres_copy_enabled | true |

detail: DELETE -> true; table rows for the tag 2 -> 0. Endpoint entries carrying the tag: 2 -> 0. The endpoint reads the table: erasing the rows blinds the admin API too, so it is a window rather than a second copy.

## A08c - write and delete routes on the admin audit endpoint (info)

| key | value |
|---|---|
| delete_status | 405 |
| post_status | 405 |

detail: DELETE /auth/v1/admin/audit -> HTTP 405 (); POST -> HTTP 405 (). A service-key holder has no API path to the audit trail's contents; the database password is the path.

## A08z - cleanup (pass)

_no measurements_

detail: test user deleted

## A09a - install a chained mirror fed by a trigger on the audit table (pass)

| key | value |
|---|---|
| install_ok | true |
| triggers_on_audit_table | 1 |

detail: schema, chained table, SECURITY DEFINER capture function, verifier and trigger created; 1 trigger now on auth.audit_log_entries. A tenant CAN attach a trigger to the Auth server's own table - the ACL grants postgres the TRIGGER privilege.

## A09b - a real auth event reaches the mirror through the trigger (pass)

| key | value |
|---|---|
| create_status | 200 |
| login_status | 200 |
| mirrored | true |
| mirror_lag_s | 0 |
| mirror_rows | 2 |
| verify_checked | 2 |
| verify_break | none |

detail: admin user create -> HTTP 200, password login -> HTTP 200; the mirror captured the tagged entry 0s later. Mirror holds 2 rows (seq 1..2); verifier: 2 rows checked, first break none. GoTrue writes as supabase_auth_admin and the SECURITY DEFINER function still records it.

## A09c - delete one mirror row: does the verifier notice (info)

| key | value |
|---|---|
| deleted_seq | 2 |
| verify_break_after_delete | none |
| verify_checked | 1 |

detail: deleted mirror seq 2; verifier now reports first break at seq none over 1 rows. A row removed from the middle of a chain cannot be removed quietly - the next row's prev_hash no longer recomputes.

## A09d - the same identity rebuilds the chain and the verifier goes quiet (pass)

| key | value |
|---|---|
| rebuild_ok | true |
| verify_break_after_rebuild | none |
| head_hash_changed | false |
| head_before | ec83cf53fde2650b |
| head_after | ec83cf53fde2650b |
| postgres_copy_enabled | true |

detail: rehash of every row -> true; verifier now reports first break none over 1 rows. The head hash moved from ec83cf53fde2... to ec83cf53fde2..., so the ONE thing that survives an owner with a rehash loop is a head hash that was copied out of the database before the tampering. In-database immutability against the database owner does not exist; off-box anchoring is the control.

## A09z - cleanup (pass)

_no measurements_

detail: trigger and schema dropped (0 user triggers left on the audit table); test user deleted
