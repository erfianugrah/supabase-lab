# Facts from audit-integrity - 2026-09-08 06:56:47 UTC

Source artifact: run started 2026-09-08T06:56:47.499Z, finished 2026-09-08T06:57:23.471Z, vantage local, region ap-southeast-1, lab commit 9125b00. Project ref omitted on purpose.

## A10a - physical copies available on this project (info)

| key | value |
|---|---|
| http | 200 |
| pitr_enabled | false |
| walg_enabled | true |
| logical_backups | 1 |
| physical_window_present | false |

detail: GET database/backups -> HTTP 200: pitr_enabled=false, walg_enabled=true, 1 logical backups listed, physical window none .. none. A fresh project has no history yet, which is itself the finding: the backstop only exists after the plan's first backup, and PITR is an add-on rather than a default.

## A10b - restore levers on the Management API (info)

| key | value |
|---|---|
| backup_paths | 6 |

detail: 6 backup paths: backups, backups/restore-pitr, backups/restore-point, backups/restore, backups/schedule, backups/undo. Any of them is reachable with the same PAT that can delete the audit rows, so the backstop is not protected from the actor it protects against - unless the restore lands in a DIFFERENT project (backup.restore_to_new_project in the entitlements A07 reads).

## A10c - what the recovery path costs before anyone starts it (info)

| key | value |
|---|---|
| addons_status | 200 |
| pitr_variants | 3 |
| pitr_prices | 7 days=$100/month \| 14 days=$200/month \| 28 days=$400/month |

detail: GET billing/addons -> HTTP 200; PITR variants: 7 days=$100/month, 14 days=$200/month, 28 days=$400/month. A restore point does not exist the moment the addon is applied: it needs a base backup to have landed, and A10a reads no physical window on a fresh project. So the recovery path is a monthly charge plus a wait of indeterminate length, which is why the restore-and-diff is flagged and not run.

## A13a - control-plane control and API-side SQL, fired at recorded times (pass)

| key | value |
|---|---|
| control_status | 200 |
| control_at | 2026-09-08T06:56:48.376Z |
| api_sql_status | 201 |
| api_sql_at | 2026-09-08T06:56:48.602Z |
| witness_seen | true |
| editor_step_ran | true |
| search_window_s | 34 |

detail: control PATCH config/auth (a value patched to itself) -> HTTP 200 at 2026-09-08T06:56:48.376Z. API-side SQL (POST /database/query, a DELETE on auth.audit_log_entries) -> HTTP 201 at 2026-09-08T06:56:48.602Z. Independent execution witness: a DDL statement with nonce a13witog31h7hg appeared in postgres_logs 34s later, so the API SQL path was live at that moment. Dashboard SQL Editor marker "a13probe" WAS found in postgres_logs (1s), so the operator step ran.

## A13b - what the organization audit log showed (operator-supplied) (skip)

| key | value |
|---|---|
| observation_supplied | false |
| observation | none |

detail: not supplied. Run the operator step in the module header, read the organization audit log for the two timestamps in A13a, and re-run with PVLAB_AUDIT_OBSERVED set. Without it this experiment has no evidence either way about SQL execution in the organization audit log.
