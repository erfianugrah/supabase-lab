# Facts from audit-integrity - 2026-09-08 05:40:21 UTC

Source artifact: run started 2026-09-08T05:40:21.979Z, finished 2026-09-08T05:47:04.599Z, vantage local, region ap-southeast-1, lab commit 60e7194. Project ref omitted on purpose.

## A00 - control: fresh project healthy, keys present, version recorded (pass)

| key | value |
|---|---|
| healthy | true |
| health_wait_s | 0 |
| pg_version | 17.6 |
| project_age_min | 16 |
| key_generations | 4 |

detail: health: auth=ACTIVE_HEALTHY rest=ACTIVE_HEALTHY db=ACTIVE_HEALTHY after 0s. Keys: anon, service_role, publishable, secret. Postgres 17.6, project 16 min old at first probe.

## A01a - auth.audit_log_entries: owner, ACL and shape (info)

| key | value |
|---|---|
| owner | supabase_auth_admin |
| fk_count | 0 |
| user_triggers | 0 |
| rows_at_start | 0 |
| acl | {supabase_auth_admin=arwdDxtm/supabase_auth_admin,dashboard_user=arwdDxtm/supabase_auth_admin,postgres=ar*wdDxtm/supabase_auth_admin} |

detail: owner=supabase_auth_admin; acl={supabase_auth_admin=arwdDxtm/supabase_auth_admin,dashboard_user=arwdDxtm/supabase_auth_admin,postgres=ar*wdDxtm/supabase_auth_admin}; columns: instance_id uuid, id uuid, payload json, created_at timestamp with time zone, ip_address character varying; constraints: audit_log_entries_pkey:p (0 foreign keys, 0 user triggers); 0 rows at first read.

## A01b - which roles hold delete/truncate on the audit table (info)

| key | value |
|---|---|
| roles_probed | 10 |
| roles_with_delete | 4 |
| roles_with_select | 5 |
| service_role_select | false |
| service_role_delete | false |
| read_only_user_select | true |
| read_only_user_delete | false |
| postgres_delete | true |
| postgres_truncate | true |

detail: 4 of 10 roles can delete rows: postgres, dashboard_user, supabase_admin, supabase_auth_admin. 5 can read: postgres, dashboard_user, supabase_admin, supabase_auth_admin, supabase_read_only_user. The three API roles (anon, authenticated, service_role) hold: service_role=none, anon=none, authenticated=none.

## A01c - role attributes: who can log in, who bypasses RLS, who inherits what (info)

| key | value |
|---|---|
| roles_present | 10 |
| read_only_user_present | true |
| read_only_user_memberships | pg_monitor,pg_read_all_data |

detail: 10 of 10 roles exist. supabase_read_only_user exists (login=true, member_of=pg_monitor,pg_read_all_data). dashboard_user login=false.

## A01d - platform logging defaults on a fresh project (info)

| key | value |
|---|---|
| log_statement | ddl |
| log_min_duration_statement | -1 |
| log_connections | off |
| pgaudit_installed | no |
| pgaudit_available | 17.1 |
| pgaudit_log | none |

detail: log_statement=ddl, log_min_duration_statement=-1, log_connections=off, log_disconnections=off, pgaudit installed=no (available 17.1), pgaudit.log=none. With log_statement=ddl and no pgaudit, a DELETE is not a logged statement class - A03 runs one.

## A07a - audit and log retention per plan, from the entitlement API (pass)

| key | value |
|---|---|
| orgs_probed | 3 |
| team_plan | team |
| team_audit_log_days | 62 days |
| team_log_retention_days | 28 days |
| team_backup_retention_days | 14 days |
| team_audit_log_drains | true |
| team_log_drains | true |
| pro_plan | pro |
| pro_audit_log_days | no-access:0 days |
| pro_log_retention_days | 7 days |
| pro_backup_retention_days | 7 days |
| pro_audit_log_drains | false |
| pro_log_drains | true |
| free_plan | free |
| free_audit_log_days | no-access:0 days |
| free_log_retention_days | 1 days |
| free_backup_retention_days | no-access:0 days |
| free_audit_log_drains | false |
| free_log_drains | false |

detail: team (plan=team, HTTP 200, 64 entitlements): security.audit_logs_days=62 days; log.retention_days=28 days; backup.retention_days=14 days; audit_log_drains=true; log_drains=true; pitr.available_variants=pitr_7\|pitr_14\|pitr_28; security.private_link=true; security.soc2_report=true \|\| pro (plan=pro, HTTP 200, 64 entitlements): security.audit_logs_days=no-access:0 days; log.retention_days=7 days; backup.retention_days=7 days; audit_log_drains=false; log_drains=true; pitr.available_variants=pitr_7\|pitr_14\|pitr_28; security.private_link=false; security.soc2_report=false \|\| free (plan=free, HTTP 200, 64 entitlements): security.audit_logs_days=no-access:0 days; log.retention_days=1 days; backup.retention_days=no-access:0 days; audit_log_drains=false; log_drains=false; pitr.available_variants=none; security.private_link=false; security.soc2_report=false

## A07b - the platform audit log on the Management API (info)

| key | value |
|---|---|
| spec_paths | 115 |
| audit_paths | 0 |

detail: 115 paths in the /v1 spec; 0 mention audit - the platform audit log has no read path, no export path and no delete path on the public API. It is Dashboard-only, which is why no tenant credential can rewrite it and also why nobody can pull it into a SIEM.

## A10a - physical copies available on this project (info)

| key | value |
|---|---|
| http | 200 |
| pitr_enabled | false |
| walg_enabled | true |
| logical_backups | 1 |
| physical_window_present | false |

detail: GET database/backups -> HTTP 200: pitr_enabled=false, walg_enabled=true, 1 logical backups listed, physical window none .. none. A fresh project has no history yet, which is itself the finding: the backstop only exists after the plan's first backup, and PITR is an add-on rather than a default.

## A10b - restore levers on the Management API (info)

| key | value |
|---|---|
| backup_paths | 6 |

detail: 6 backup paths: backups, backups/restore-pitr, backups/restore-point, backups/restore, backups/schedule, backups/undo. Any of them is reachable with the same PAT that can delete the audit rows, so the backstop is not protected from the actor it protects against - unless the restore lands in a DIFFERENT project (backup.restore_to_new_project in the entitlements A07 reads).

## A02a - matrix: role x operation against auth.audit_log_entries (info)

| key | value |
|---|---|
| cells | 30 |
| postgres_allowed | 5 |
| dashboard_user_allowed | none |
| read_only_user_allowed | none |
| service_role_allowed | 0 |
| authenticated_allowed | 0 |
| anon_allowed | 0 |
| denial_kinds | 3 |

detail: service_role: nothing; authenticated: nothing; anon: nothing; supabase_read_only_user: nothing; dashboard_user: nothing; postgres: select/insert/update/delete/truncate. Distinct denial: permission denied for table audit_log_entries \| permission denied to set role "dashboard_user" \| permission denied to set role "supabase_read_only_user".

## A02b - forging an audit entry GoTrue never wrote (info)

| key | value |
|---|---|
| forge_accepted | true |
| rows_read_back | 1 |
| backdated | true |
| removed | true |

detail: insert accepted; read back 1 row with a chosen actor (<email>), a chosen source IP and a created_at three days in the past. Removed afterwards: true. The table has no FK to auth.users and no trigger, so nothing reconciles a forged row against a real user.

## A02c - rewriting an existing entry in place (source IP and action) (info)

| key | value |
|---|---|
| update_accepted | true |
| rows_rewritten | 1 |
| table_rows_after | 0 |

detail: UPDATE accepted: the row's source IP and its action were both rewritten (1 row now reads action=logout from 10.0.0.1). An audit row is a plain heap row - no immutability, no version, no updated_at to notice the edit by. Table left at 0 rows.

## A03a - control: a DDL statement is findable in postgres_logs by its nonce (pass)

| key | value |
|---|---|
| control_found | true |
| control_lag_s | 191 |
| ddl_status | 201 |

detail: create/drop table -> HTTP 201; the statement text appeared in postgres_logs 191s later. The search works and ingestion is live, so a miss below is a miss.

## A03b - a bare DELETE on the audit table in postgres_logs (info)

| key | value |
|---|---|
| delete_status | 201 |
| delete_logged | false |
| searched_s | 191 |

detail: delete -> HTTP 201; searched postgres_logs for its nonce for 191s: not found. log_statement=ddl does not cover DML, so the erasure of an audit row is not itself a logged statement.

## A03c - a bare TRUNCATE on the audit table in postgres_logs (info)

| key | value |
|---|---|
| truncate_status | 201 |
| truncate_logged | false |
| searched_s | 191 |

detail: truncate -> HTTP 201; searched for 191s: not found. TRUNCATE reads as DDL to a human and as `mod` to log_statement, so the fastest way to empty the table is also unlogged.

## A03d - provenance on a statement the platform ran for you (info)

| key | value |
|---|---|
| logged_role | postgres |
| logged_application | mgmt-api |
| provenance_comment | true |

detail: the control line is attributed to role=postgres, application_name=mgmt-api, and the platform appended a provenance comment naming the calling route and the acting identity (3 lines: -- source: POST /v1/projects/:ref/database/query -- user: pat:<id> -- date: 2026-09-08T05:40:26.577Z). Every tenant path - Dashboard, Management API, MCP - arrives as this one database role, so per-person attribution is not in Postgres.

## A04a - log_statement through the Management API (info)

| key | value |
|---|---|
| config_keys | 0 |
| log_statement_settable | false |
| log_connections_settable | false |
| put_log_statement_status | 400 |

detail: GET config/database/postgres -> HTTP 200, 0 keys, log_statement absent (log_connections absent). PUT log_statement=mod -> HTTP 400: {"message":": Unrecognized key: \"log_statement\""}. Statement-level DML logging is therefore a pgaudit job, not a config toggle.

## A04b - pgaudit session mode catches the delete and the truncate (pass)

| key | value |
|---|---|
| extension_installed | true |
| delete_audited | true |
| delete_audit_lag_s | 32 |
| truncate_audited | true |
| truncate_audit_lag_s | 32 |
| restart_needed | false |

detail: create extension -> true; alter role postgres set pgaudit.log to 'write' -> true (rolconfig now pgaudit.log=write); no restart performed. DELETE -> HTTP 201: AUDIT line found 32s later. TRUNCATE -> HTTP 201: AUDIT line found 32s later. Both statements ran in sessions opened AFTER the ALTER ROLE, which is what makes a role-level GUC apply without a reboot.

## A04c - pgaudit object mode: audit this one table, log nothing else (pass)

| key | value |
|---|---|
| auditor_created | true |
| object_mode_set | true |
| session_logging_off | true |
| delete_audited_object_mode | true |
| object_audit_lag_s | 33 |

detail: auditor role -> true; grant on the audit table -> true; pgaudit.role=a04_auditor -> true; pgaudit.log back to none -> true. DELETE -> HTTP 201: AUDIT line found 33s later (AUDIT: OBJECT,1,1,WRITE,DELETE,TABLE,auth.audit_log_entries,"delete from auth.audit_log_en). Object mode is the form worth running continuously - it says nothing about the other 99% of traffic.

## A04d - log_connections through the API (pass)

| key | value |
|---|---|
| put_status | 200 |
| log_connections_after | off |

detail: PUT log_connections=true -> HTTP 200; the session reports log_connections=off (a running session shows the value it started with, so the change lands for new connections). This is the lever that distinguishes a tenant path from a direct psql session, and it is off by default.

## A04z - cleanup (pass)

_no measurements_

detail: restored after the object phase: pgaudit not installed, postgres rolconfig pgaudit entries none, log_connections back to false.

## A06a - audit-log keys in the documented auth config (info)

| key | value |
|---|---|
| config_keys | 243 |
| audit_keys | 1 |
| get_status | 200 |
| key_present_before | true |
| key_before | true |

detail: GET config/auth -> HTTP 200, 243 keys returned, 1 of them mention audit (audit_log_disable_postgres=true). audit_log_disable_postgres is present and reads true.

## A06b - PATCH audit_log_disable_postgres through the Management API (info)

| key | value |
|---|---|
| patch_status | 200 |
| patch_target | false |
| key_after_patch | true |
| write_took | false |

detail: asked for audit_log_disable_postgres=false (the opposite of the true it held). PATCH -> HTTP 200; on re-read the key is true. A 2xx that leaves the value unchanged: the switch is accepted and ignored on the public API, so it is Dashboard-only (Authentication -> Configuration -> Audit Logs).

## A06c - does a new auth event still reach the table (pass)

| key | value |
|---|---|
| create_status | 200 |
| login_status | 200 |
| create_attempts | 1 |
| rows_before | 0 |
| rows_after | 0 |
| tagged_row_appeared | false |
| postgres_copy_default | off |

detail: admin user create -> HTTP 200 in 1 attempt(s); password login -> HTTP 200. Table rows 0 -> 0; a row tagged with this run did not appear within 120s. On a fresh project the Postgres copy is OFF by default.

## A06z - cleanup (pass)

_no measurements_

detail: auth config PATCH reverted; test user deleted

## A12a - organization members and their roles, per plan (info)

| key | value |
|---|---|
| team_members | 1 |
| team_roles | Owner |
| pro_members | 3 |
| pro_roles | Owner\|Administrator |
| free_members | 1 |
| free_roles | Owner |

detail: team: HTTP 200, 1 members, roles {"Owner":1}, 0 with MFA \|\| pro: HTTP 200, 3 members, roles {"Owner":1,"Administrator":2}, 0 with MFA \|\| free: HTTP 200, 1 members, roles {"Owner":1}, 0 with MFA

## A12b - read_only login role: connect, read the audit table, delete from it (pass)

| key | value |
|---|---|
| mint_status | 201 |
| ttl_seconds | 300 |
| connected | true |
| current_user | cli_login_supabase_read_only_user |
| can_read_audit | false |
| can_delete_audit | false |

detail: POST cli/login-role {read_only:true} -> HTTP 201, ttl 300s. Connected through the session pooler as <role>.<ref>: current_user = cli_login_supabase_read_only_user. select on auth.audit_log_entries: denied (ERROR: permission denied for schema auth LINE 1: select count(*) from auth.audit_log_entries ^). delete: denied (ERROR: permission denied for schema auth LINE 1: delete from auth.audit_log_entries ^).

## A12c - read-write login role: connect, read the audit table, delete from it (info)

| key | value |
|---|---|
| mint_status | 201 |
| ttl_seconds | 300 |
| connected | true |
| current_user | cli_login_postgres |
| can_read_audit | false |
| can_delete_audit | false |

detail: POST cli/login-role {read_only:false} -> HTTP 201, ttl 300s. Connected through the session pooler as <role>.<ref>: current_user = cli_login_postgres. select on auth.audit_log_entries: denied (ERROR: permission denied for schema auth LINE 1: select count(*) from auth.audit_log_entries ^). delete: denied (ERROR: permission denied for schema auth LINE 1: delete from auth.audit_log_entries ^).

## A12d - revoking the minted login roles (pass)

| key | value |
|---|---|
| delete_status | 200 |
| roles_minted | 2 |

detail: DELETE cli/login-role -> HTTP 200 ({"message":"ok"}); 2 role(s) had been minted. A connection attempt with a wrong password afterwards: psql: error: connection to server at "<pooler-host>" (<ip>), port 5432 failed: FATAL: password authentication failed for user "cli_login_supabase_read_only_user".
