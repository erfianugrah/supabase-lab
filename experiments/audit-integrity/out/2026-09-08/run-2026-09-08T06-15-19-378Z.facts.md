# Facts from audit-integrity - 2026-09-08 06:15:19 UTC

Source artifact: run started 2026-09-08T06:15:19.378Z, finished 2026-09-08T06:15:27.515Z, vantage local, region ap-southeast-1, lab commit 60e7194. Project ref omitted on purpose.

## A09a - install a chained mirror fed by a trigger on the audit table (pass)

| key | value |
|---|---|
| install_ok | true |
| triggers_on_audit_table | 1 |

detail: schema, chained table, SECURITY DEFINER capture function, verifier and trigger created; 1 trigger now on auth.audit_log_entries. A tenant CAN attach a trigger to the Auth server's own table - the ACL grants postgres the TRIGGER privilege.

## A09b - a real auth event reaches the mirror through the trigger (pass)

| key | value |
|---|---|
| create_status | 200 |
| login_status | 200 |
| mirrored | true |
| mirror_lag_s | 0 |
| mirror_rows | 4 |
| verify_checked | 4 |
| verify_break | none |

detail: admin user create -> HTTP 200, password login -> HTTP 200; the mirror captured the tagged entry 0s later. Mirror holds 4 rows (seq 1..4); verifier: 4 rows checked, first break none. GoTrue writes as supabase_auth_admin and the SECURITY DEFINER function still records it.

## A09e - delete the LAST mirror row: does the verifier notice (pass)

| key | value |
|---|---|
| tail_seq | 4 |
| mirror_rows_before_tail_cut | 4 |
| verify_break_after_tail_delete | none |
| verify_checked_tail | 3 |

detail: deleted the tail (seq 4) of a 4-row mirror; verifier reports first break none over 3 rows. Each row links to the one BEFORE it, so removing the newest rows leaves a chain that still recomputes end to end. A chain detects an edit or an interior removal; it does not detect truncation, and truncation is what someone hiding a recent event would do.

## A09c - delete an INTERIOR mirror row: does the verifier notice (pass)

| key | value |
|---|---|
| deleted_seq | 2 |
| rows_at_interior_cut | 3 |
| verify_break_after_delete | 3 |
| verify_checked | 2 |

detail: deleted interior mirror seq 2 of 3 remaining; verifier now reports first break at seq 3 over 2 rows. A row removed from the middle cannot be removed quietly - the next row's prev_hash no longer recomputes.

## A09d - the same identity rebuilds the chain and the verifier goes quiet (pass)

| key | value |
|---|---|
| rebuild_ok | true |
| verify_break_after_rebuild | none |
| head_hash_changed | true |
| head_before | 5d462b0c189312fb |
| head_after | 2eb685578caa86b3 |
| postgres_copy_enabled | true |

detail: rehash of every row -> true; verifier now reports first break none over 2 rows. The head hash moved from 5d462b0c1893... to 2eb685578caa..., so the ONE thing that survives an owner with a rehash loop is a head hash that was copied out of the database before the tampering. In-database immutability against the database owner does not exist; off-box anchoring is the control.

## A09z - cleanup (pass)

_no measurements_

detail: trigger and schema dropped (0 user triggers left on the audit table); 2 test user(s) deleted
