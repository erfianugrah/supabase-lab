# Facts from self-hosted-auth - 2026-09-02 06:58:23 UTC

Source artifact: run started 2026-09-02T06:58:23.188Z, finished 2026-09-02T06:58:47.332Z, vantage local, region ap-southeast-1, lab commit f5d06cb. Project ref omitted on purpose.

## SH01a - role map for a self-hosted GoTrue (pass)

| key | value |
|---|---|
| auth_admin_can_login | 1 |
| postgres_member_of_auth_admin | 0 |
| postgres_superuser | 0 |
| auth_schema_owner | supabase_admin |
| pg_auth_usage | 1 |
| pg_auth_create | 0 |
| pg_users_insert | 1 |
| pg_refresh_tokens_insert | 1 |
| pg_sessions_insert | 1 |
| pg_schema_migrations_insert | 0 |
| alter_auth_admin_error | Failed to run sql query: ERROR:  42501: "supabase_auth_admin" is a reserved role, only superusers can modify it |
| grant_auth_admin_error | Failed to run sql query: ERROR:  42501: "supabase_auth_admin" role memberships are reserved, only superusers can grant them |
| auth_admin_rolconfig | search_path=auth idle_in_transaction_session_timeout=60000 log_statement=none |
| postgres_rolconfig | search_path="\$user", public, extensions |

detail: supabase_auth_admin is reserved (ALTER: "Failed to run sql query: ERROR:  42501: "supabase_auth_admin" is a reserved role, only superusers can modify it "); postgres can write auth.users/refresh_tokens but has no CREATE on auth and no INSERT on schema_migrations

## SH01b - auth.schema_migrations as the platform left it (info)

| key | value |
|---|---|
| rows | 77 |
| latest | 20260625000000 |
| earliest | 00 |

detail: 77 rows, 00 .. 20260625000000

## SH01c - signing keys: what the managed side signs with, and what still verifies (info)

| key | value |
|---|---|
| keys | HS256:previously_used\|ES256:in_use |
| in_use_alg | ES256 |
| hs256_status | previously_used |
| jwt_secret_chars | 88 |

detail: in_use ES256; HS256 key previously_used; legacy jwt_secret 88 chars via GET /postgrest

## SH01d - managed GoTrue version (info)

| key | value |
|---|---|
| managed_version | v2.196.0 |
| self_hosted | configured |

detail: managed v2.196.0

## SH02a - self-hosted GoTrue up (pass)

| key | value |
|---|---|
| self_hosted_version | v2.196.0 |

detail: self-hosted v2.196.0 at http://localhost:9999

## SH02b - user created through the self-hosted GoTrue is visible to the managed one (pass)

| key | value |
|---|---|
| create_status | 200 |
| managed_list_status | 200 |
| present_in_managed | 1 |

detail: self-hosted admin create 200; managed admin list 200, present=true

## SH02c - self-hosted password grant and token shape (pass)

| key | value |
|---|---|
| status | 200 |
| alg | HS256 |
| kid | 8043c259 |
| iss | https://<ref>.supabase.co/auth/v1 |
| aud | authenticated |
| role | authenticated |
| ttl_s | 3600 |

detail: alg HS256, iss https://<ref>.supabase.co/auth/v1, aud authenticated, role authenticated, ttl 3600s

## SH02d - managed Auth and PostgREST accept the self-hosted token (fail)

| key | value |
|---|---|
| managed_user_status | 200 |
| managed_user_code | none |
| rest_status | 401 |
| rest_rows | 0 |
| rest_code | PGRST301 |
| anon_control_rows | 0 |

detail: managed /user 200; PostgREST authenticated-only read 401 rows=0 PGRST301; anon control rows=0

## SH02e - managed password grant for the same user (shared hash) and its token shape (pass)

| key | value |
|---|---|
| status | 200 |
| alg | ES256 |
| kid | c7274695 |
| same_sub | 1 |
| same_iss | 1 |

detail: alg ES256 kid c7274695, iss https://<ref>.supabase.co/auth/v1, same sub=true

## SH02f - the managed (ES256) token against the self-hosted GoTrue (info)

| key | value |
|---|---|
| self_hosted_user_status | 200 |
| self_hosted_user_code | none |

detail: self-hosted GoTrue verified the managed token

## SH02z - cleanup: delete the probe user via the managed admin API (pass)

| key | value |
|---|---|
| delete_status | 200 |

detail: HTTP 200

## SH03a - self-hosted refresh token redeemed by the managed GoTrue (pass)

| key | value |
|---|---|
| status | 200 |
| code | none |
| new_alg | ES256 |
| same_session | 1 |

detail: 200; new access token alg ES256

## SH03b - managed refresh token redeemed by the self-hosted GoTrue (pass)

| key | value |
|---|---|
| status | 200 |
| code | none |
| new_alg | HS256 |
| same_session | 1 |

detail: 200; new access token alg HS256 (re-signed with the self-hosted key)

## SH03c - reuse detection is per schema, not per instance (info)

| key | value |
|---|---|
| reused_parent_status | 200 |
| reused_parent_code | none |
| child_from_self_hosted_status | 200 |
| child_code | none |

detail: already-redeemed managed RT -> managed: 200 (parent tolerated inside the reuse interval); the child RT minted by the self-hosted side -> managed: 200

## SH03z - cleanup: delete the probe user (pass)

| key | value |
|---|---|
| delete_status | 200 |

detail: HTTP 200

## SH04a - advertised JWKS (mode=jwks) (info)

| key | value |
|---|---|
| mode | jwks |
| self_hosted_status | 200 |
| self_hosted_keys | EC/ES256:verify |
| managed_keys | EC/ES256:verify |

detail: self-hosted 200: EC/ES256:verify; managed 200: EC/ES256:verify

## SH04b - managed token verified by the self-hosted GoTrue (pass)

| key | value |
|---|---|
| mode | jwks |
| status | 200 |
| code | none |
| managed_alg | ES256 |

detail: managed ES256 token -> self-hosted /user 200 with the platform's public key supplied as a verify-only JWK

## SH04c - self-hosted token still accepted by the managed GoTrue and PostgREST (fail)

| key | value |
|---|---|
| user_status | 200 |
| user_code | none |
| rest_status | 401 |
| rest_rows | 0 |
| rest_code | PGRST301 |
| self_alg | HS256 |
| self_kid | 8043c259 |

detail: self-hosted HS256 token (kid 8043c259) -> managed /user 200; PostgREST 401 rows=0 PGRST301

## SH04z - cleanup: delete the probe user (pass)

| key | value |
|---|---|
| delete_status | 200 |

detail: HTTP 200

## SH05a - before the revoke: self-hosted token trusted (fail)

| key | value |
|---|---|
| user_status | 200 |
| rest_status | 401 |
| rest_rows | 0 |

detail: managed /user 200; PostgREST 401 rows=0

## SH05b - PATCH the HS256 key to revoked (pass)

| key | value |
|---|---|
| status | 200 |
| key_status_after | revoked |

detail: HTTP 200

## SH05c - after the revoke: the same self-hosted token (pass)

| key | value |
|---|---|
| user_status | 403 |
| user_code | bad_jwt |
| user_flip_s | 6 |
| rest_status | 401 |
| rest_code | PGRST301 |
| rest_flip_s | 0 |

detail: managed /user 403 bad_jwt after 6s; PostgREST 401 PGRST301 after 0s

## SH05d - collateral: legacy API keys vs the new key generation, and fresh self-hosted grants (info)

| key | value |
|---|---|
| legacy_anon_rest_status | 401 |
| legacy_anon_auth_health_status | 200 |
| legacy_service_admin_status | 403 |
| publishable_rest_status | 200 |
| secret_admin_status | 200 |
| fresh_self_grant_status | 200 |

detail: legacy anon key: PostgREST 401, auth health 200; legacy service_role key: admin list 403; sb_publishable: PostgREST 200; sb_secret: admin list 200; fresh self-hosted password grant 200 (mints, but nothing managed will accept it now)

## SH05z - cleanup: delete the probe user (pass)

| key | value |
|---|---|
| admin_delete_status | 403 |
| cleaned | 1 |

detail: sql fallback (admin api HTTP 403)
