# Non-secret experiment config - committed. Secrets live in ../../secrets.tfvars
# (decrypt with `make secrets-decrypt` at the repo root).

project_name  = "lab-http-tier"
region        = "ap-southeast-1"
instance_size = "micro"
